pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                // put build commands here later
            }
        }

        stage('Test') {
            steps {
                echo 'Testing..'
                // put test commands here later
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying....'
                // put deploy commands here later
            }
        }
    }
}
